# DPF is symlinked in by the build (or by your local dev setup),
# never committed.
plugin/dpf
plugin/dpf/

# DPF / make build artefacts
plugin/build/
plugin/bin/
plugin/*.o
plugin/*.d

# macOS / editor noise
.DS_Store
*.swp
